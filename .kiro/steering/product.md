Luật chung: xem `AGENTS.md` ở gốc repo. Bản đồ tài liệu: `docs/README.md`.
Spec hiệu lực của Kiro: `.kiro/specs/nen-tang-da-don-vi/` (bản sao đọc được có frontmatter ở `docs/specs/`).

- **DYC:** chủ quản, admin global — toàn quyền đọc mọi dữ liệu nghiệp vụ của mọi đơn vị và mọi module.
- **TCKT (Ban Tổ chức – Kiểm tra):** đơn vị pilot của module Điều hành, 5 role nội bộ (`admin`, `vice_admin`, `leader`, `vice_leader`, `member`). Từ tổ phó (`vice_leader`) trở lên có quyền CTD (role `tckt`).
- **VP Đoàn, Chi bộ, ĐT/LCĐ, Sinh viên:** module Công tác Đảng (`services/ctd-api/`). ĐT/LCĐ ở GĐ1 là dữ liệu giả.
- Chi tiết BA: `docs/ba/tong-quan-nen-tang.md`, `docs/ba/co-cau-don-vi-va-role.md`.
